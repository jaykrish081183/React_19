const DefaultProps = ({name="New User"}) => {
    return (
        <div>
            <h1>Hi, {name}</h1>
            <h1>Default Props Example</h1>
            <p>This component demonstrates the use of default props in React.</p>
        </div>
    )
}

export default DefaultProps;